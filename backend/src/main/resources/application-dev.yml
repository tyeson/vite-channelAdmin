server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/channel_admin_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Szna123456.

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.channel.admin: DEBUG
    org.springframework.security: DEBUG
